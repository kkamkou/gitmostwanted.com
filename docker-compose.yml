version: '3.7'
services:
  gmw_db:
    image: mysql:latest
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=gitmostwanted
      - MYSQL_USER=gitmostwanted
      - MYSQL_PASSWORD=gitmostwanted_pwd
    command: mysqld --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci
    restart: unless-stopped

  gmw_redis:
    image: redis:latest
    restart: unless-stopped

  gmw_web:
    build: .
    user: gitmostwanted
    entrypoint: ["sh", "/opt/gitmostwanted/entrypoint.sh", "web"]
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - .:/opt/gitmostwanted
    environment:
      - GMW_APP_SETTINGS=/opt/gitmostwanted/instance.cfg
    depends_on:
      - gmw_db
      - gmw_redis

  gmw_celery:
    build: .
    user: gitmostwanted
    restart: unless-stopped
    entrypoint: ["sh", "/opt/gitmostwanted/entrypoint.sh", "celery"]
    environment:
      - GMW_APP_SETTINGS=/opt/gitmostwanted/instance.cfg
    volumes:
      - .:/opt/gitmostwanted
    links:
      - gmw_db
      - gmw_redis
